# HIVIEW 使用手册

## 1. 简介

**HIVIEW** 是一款基于海思 HiSilicon 芯片平台的视频处理综合系统，由深圳市海图视讯科技有限公司（HIVIEW-TECH）开发和维护。系统提供完整的嵌入式视频解决方案，支持设备接入、视频编解码、网络传输、Web 管理、AI 算法分析等多种功能，广泛应用于工业视觉、安防监控、智能感知等场景。

本手册针对 `HIVIEW-master` 工程的最新版本，面向现场使用和调试人员，从网络配置、Web 界面操作、各功能模块使用、参数调优到故障排查，进行全流程说明。

---

## 2. 系统概述

### 2.1 支持芯片

| 芯片系列 | 代表型号 |
| --- | --- |
| Hi3516x | Hi3516D / Hi3516DV300 |
| Hi3519x | Hi3519A / Hi3519D |
| Hi3559x | Hi3559A |
| Hi3536x | Hi3536D |

### 2.2 主要功能模块

| 模块 | 功能 |
| --- | --- |
| **BSP** | 底层驱动、网络、WiFi、VPN、系统升级 |
| **MPP** | 海思媒体平台（VI、VPSS、VENC、GPU 等） |
| **CODEC** | H.264 / H.265 / MJPEG 视频编码，AAC / PCMA / PCMU 音频编码 |
| **RTSPS** | RTSP 视频服务器 / 客户端 / Push 模式 |
| **WEBS** | HTTP / HTTPS / WebSocket / WebRTC 服务 |
| **APP / GUI** | 上层应用与图形界面 |
| **REC / fMP4** | 视频录制、fMP4 封装 |
| **RTMPS** | RTMP Push 推流 |
| **SIPS / GB28181** | SIP / 国标 28181 接入 |
| **ONVIF** | ONVIF 协议（NVT / NVC） |
| **SRTS** | SRT / UDP / RTP / SRTP 传输 |
| **SVP** | AI 视觉算法（YOLO 目标检测、LPR 车牌识别、光斑质心提取等） |
| **UVC** | USB 视频设备（H.264 / MJEPG） |

### 2.3 系统架构

```
传感器 Sensor → VI（视频输入）→ VPSS（图像处理）→ VENC（编码）
                                                    ↓
                                          RTSPS / RTMPS / SRTS
                                                    ↓
                                             网络传输 / 存储
                                              ↓
                                     Web 配置管理 / 播放监控
                                     SVP AI 算法分析
```

---

## 3. 使用前准备

### 3.1 硬件环境

- 开发板（Hi3516D / Hi3516DV300 等）
- PC 电脑或笔记本电脑
- 网线（PC 与开发板直连或在同一局域网内）
- USB-TTL 串口转接板（用于查看串口日志，可选）

### 3.2 网络连接

PC 与开发板需在同一网段。推荐配置：

```text
PC / 电脑 IP:   192.168.0.1
开发板 IP:      192.168.0.2
```

在 PC 上验证网络连通：

```bash
ping 192.168.0.2
```

若无法 ping 通，检查以下项目：

- 网线是否已连接
- PC 网卡 IP 是否为 `192.168.0.x`
- 开发板 IP 是否正确设置为 `192.168.0.2`
- 若使用虚拟机，网络模式是否为**桥接**
- PC 防火墙是否拦截 ICMP

### 3.3 打开 Web 管理界面

在浏览器地址栏输入：

```text
http://192.168.0.2
```

进入 HIVIEW Web 管理界面。如果页面无法打开，优先确认网络连通性，再确认板端 `webs.exe` 进程已正常运行。

---

## 4. Web 界面详解

浏览器打开 `http://192.168.0.2` 后，进入主要页面。

### 4.1 主页面

主页展示设备基本信息，包括：
- 设备型号与固件版本
- 网络状态
- 视频通道预览

### 4.2 Live 实时画面

进入路径：

```text
Live
```

在该页面可查看：
- 实时视频画面
- 视频状态（分辨率、帧率）
- 设备当前状态信息

若开启了光斑质心算法，画面中会显示：
- 红色 `X` 标记当前光斑质心位置
- 左上角坐标文本：`CENTROID: x=..., y=..., area=..., clarity=..., exp=..., target=..., ae=..., size=...`
- 右上角半透明清晰度曲线窗口，显示最近约 10 秒的清晰度变化

### 4.3 Config 配置页面

进入路径：

```text
Config
```

左侧边栏按功能分类列出所有可配置项。点击任一配置项后，右侧显示配置内容，支持：

- **GET**：从设备读取当前配置
- **SET**：将修改后的配置写入设备

各配置项说明如下。

---

## 5. 主要配置说明

### 5.1 通用 SVP 配置

使用路径：

```text
Config -> GSF_ID_SVP_CFG
```

该配置包含 AI 算法通用参数，包括：
- SVP 算法总开关
- YOLO 目标检测参数
- LPR 车牌识别参数
- 模型路径等

操作步骤：

1. 点击 `GET` 获取当前配置
2. 修改 `data` 字段中的参数值
3. 点击 `SET` 保存
4. 再次点击 `GET` 确认保存成功

> **注意**：修改 `data` 字段以外的内容不会生效。

---

## 6. 光斑质心提取功能

### 6.1 功能概述

光斑质心提取功能可以从实时视频图像中识别和定位光斑中心（如激光光斑），通过 OpenCV 图像算法实现精确坐标提取，并同步：

- 串口输出 `CENTROID` 文本结果
- Web Live 页面显示坐标和红色 `X` 标记
- Web Live 页面显示清晰度曲线
- 支持自定义曝光闭环控制

### 6.2 开启质心算法

进入路径：

```text
Config -> GSF_ID_SVP_CENTROID_CFG
```

操作步骤：

1. 点击 `GET` 获取当前配置
2. 找到 `"centroid_alg": 0`
3. 修改为 `"centroid_alg": 1`
4. 点击 `SET` 保存
5. 再次点击 `GET`，确认 `centroid_alg` 已变为 `1`

> 若 `GET` 后依然是旧值，请确认当前页面是 `GSF_ID_SVP_CENTROID_CFG`，并确保修改的是返回 JSON 中 `data` 字段的内容。

### 6.3 Live 页面查看

进入路径：

```text
Live
```

开启成功且检测到光斑后，页面显示：

```text
CENTROID: x=..., y=..., area=..., clarity=..., exp=..., target=..., ae=..., size=...
```

其中字段含义：

| 字段 | 含义 |
| --- | --- |
| `x` | 光斑质心横坐标，单位像素 |
| `y` | 光斑质心纵坐标，单位像素 |
| `area` | 局部窗口内有效光斑面积 |
| `clarity` | 当前清晰度函数值 |
| `exp` | 当前手动曝光时间，单位微秒 |
| `target` | 目标最大灰度值 |
| `ae=down` | 亮度偏高，正在降低曝光 |
| `ae=up` | 亮度偏低，正在提高曝光 |
| `ae=hold` | 亮度在目标附近，曝光保持不变 |
| `size` | 算法处理图像尺寸 |

### 6.4 质心算法参数说明

所有质心参数都在 `GSF_ID_SVP_CENTROID_CFG` 中配置。

#### 6.4.1 算法开关

```json
"centroid_alg": 1
```

| 值 | 含义 |
| --- | --- |
| `0` | 关闭质心提取 |
| `1` | 开启质心提取 |

#### 6.4.2 抓帧通道

```json
"vpss_grp": 0,
"vpss_chn": 1
```

若 Web 红色 `X` 与实际光斑存在固定偏移，或画面有光斑但算法无结果，优先检查 `vpss_chn` 是否与 Web 预览通道一致。

#### 6.4.3 光斑阈值

```json
"threshold": 220
```

| 值 | 含义 |
| --- | --- |
| `1 ~ 254` | 固定阈值二值化 |
| 其他值（如 `0`） | 使用 OTSU 自动阈值 |

调参建议：

| 现象 | 建议 |
| --- | --- |
| 背景噪声被识别为光斑 | 增大 `threshold` |
| 光斑较暗检测不到 | 减小 `threshold` 或设为 `0` 使用 OTSU |
| 强反光区域干扰 | 增大 `threshold`，并减小 `window_size` |

#### 6.4.4 最小面积

```json
"min_area": 4
```

小于 `min_area` 的候选亮斑被认为是噪声。调参建议：

| 现象 | 建议 |
| --- | --- |
| 有很多孤立噪点 | 增大 `min_area` |
| 小光斑检测不到 | 减小 `min_area` |

#### 6.4.5 质心计算窗口

```json
"window_size": 64
```

算法先找到最亮点，再以最亮点为中心开一个 `window_size × window_size` 的局部小窗口，仅在窗口内计算质心，避免远处反光和杂散亮点干扰。

| 场景 | 建议 |
| --- | --- |
| 小光斑、噪声多 | `32` 或 `48` |
| 默认情况 | `64` |
| 离焦大光斑 | `96` 或 `128` |
| 质心被远处反光拉偏 | 减小 `window_size` |
| 质心抖动明显 | 适当增大 `window_size` |

#### 6.4.6 处理间隔

```json
"interval_ms": 100
```

控制质心线程每轮处理间隔，默认约 10 Hz。建议不要低于 `10`。

#### 6.4.7 清晰度归一化参数

```json
"clarity_gain": 1.0,
"clarity_exposure": 1.0
```

清晰度函数：

```text
F = max_gray / clarity_gain / clarity_exposure / half_area
```

若希望不同增益和曝光条件下的曲线更可比，应填入实际使用的增益和曝光值。

#### 6.4.8 清晰度曲线窗口尺寸

```json
"clarity_window_w": 360,
"clarity_window_h": 150
```

| 参数 | 默认值 | 允许范围 |
| --- | --- | --- |
| `clarity_window_w` | `360` | `180 ~ 960` |
| `clarity_window_h` | `150` | `90 ~ 540` |

Web 端会自动限制窗口不超出视频画面。

---

## 7. 曝光控制

### 7.1 曝光控制开关

```json
"ae_enable": 0
```

| 值 | 含义 |
| --- | --- |
| `0` | 关闭 ISP 自动曝光 / 自动增益，启用质心线程的手动曝光闭环 |
| `1` | 不接管曝光，恢复 ISP 原有配置 |

### 7.2 曝光控制逻辑

目标灰度：

```text
target_gray = ae_saturation × ae_target_ratio
```

默认：`target_gray = 255 × 0.8 = 204`

控制规则：

- `max_gray ≥ ae_saturation`：已饱和，曝光减半
- `max_gray > target_gray`：按比例降低曝光
- `max_gray < target_gray`：按比例提高曝光，单帧最多提高 2 倍
- `max_gray` 接近 `target_gray`：3% 死区内保持曝光不变

### 7.3 曝光相关参数

```json
{
  "ae_isp_pipe": 0,
  "ae_saturation": 255,
  "ae_target_ratio": 0.8,
  "ae_min_exp_us": 100,
  "ae_max_exp_us": 33333,
  "ae_init_exp_us": 0,
  "ae_manual_gain": 1024
}
```

| 参数 | 含义 |
| --- | --- |
| `ae_isp_pipe` | 需控制的 ISP Pipe 号，单 Sensor 通常为 `0` |
| `ae_saturation` | 最大灰度饱和值，8bit 图像为 `255` |
| `ae_target_ratio` | 目标最大灰度比例，默认 `0.8` |
| `ae_min_exp_us` | 最小曝光时间，单位微秒 |
| `ae_max_exp_us` | 最大曝光时间，单位微秒 |
| `ae_init_exp_us` | 初始曝光时间；`0` 表示读取 ISP 当前曝光作为起点 |
| `ae_manual_gain` | 固定手动增益，`1024` 表示 1× |

### 7.4 曝光调节建议

| 需求 | 操作 |
| --- | --- |
| 想让画面更暗 | 降低 `ae_target_ratio`，如改为 `0.7` |
| 限制最大曝光 | 减小 `ae_max_exp_us`，如改为 `10000` |
| 固定起始曝光 | 设置 `ae_init_exp_us` 为目标值 |

---

## 8. 串口输出

### 8.1 默认串口参数

```text
设备：   /dev/ttyAMA0
波特率： 115200
格式：   8N1
```

### 8.2 输出格式

```text
CENTROID,valid,chn,x,y,width,height,area,pts
```

示例：

```text
CENTROID,1,1,1650.33,419.97,1920,1080,47372,53648630
```

字段说明：

| 字段 | 含义 |
| --- | --- |
| `CENTROID` | 固定帧头 |
| `valid` | `1` 表示检测到有效光斑，`0` 表示未检测到 |
| `chn` | VPSS 通道号 |
| `x` | 质心横坐标，单位像素 |
| `y` | 质心纵坐标，单位像素 |
| `width` | 算法处理图像宽度 |
| `height` | 算法处理图像高度 |
| `area` | 局部窗口内有效光斑面积 |
| `pts` | VPSS 帧时间戳 |

### 8.3 修改串口设备

在 `GSF_ID_SVP_CENTROID_CFG` 中修改：

```json
"uart_dev": "/dev/ttyAMA0"
```

先查看板端串口设备：

```bash
ls -l /dev/ttyAMA* /dev/ttyS* /dev/ttyUSB* /dev/usbtty* 2>/dev/null
```

将 `uart_dev` 修改为实际使用的串口设备。

---

## 9. 推荐参数组合

### 9.1 普通小光斑

```json
{
  "centroid_alg": 1,
  "threshold": 220,
  "min_area": 4,
  "window_size": 64,
  "ae_enable": 0,
  "ae_target_ratio": 0.8
}
```

### 9.2 反光和噪声较多

```json
{
  "threshold": 240,
  "min_area": 8,
  "window_size": 48
}
```

### 9.3 光斑较大或离焦

```json
{
  "threshold": 200,
  "min_area": 20,
  "window_size": 128
}
```

### 9.4 调大清晰度曲线窗口

```json
{
  "clarity_window_w": 520,
  "clarity_window_h": 240
}
```

---

## 10. 常见问题排查

### 10.1 点击 SET 后参数没有保存

- 确认当前页面是 `GSF_ID_SVP_CENTROID_CFG`
- 修改返回结果中 `data` 字段的内容，再点击 `SET`
- 再次点击 `GET` 确认参数是否已变化

### 10.2 `centroid_alg=1` 后没有质心结果

检查以下项目：
- `Live` 页面是否有视频正常播放
- `vpss_grp` / `vpss_chn` 是否正确
- `threshold` 是否过高
- `min_area` 是否过大
- 光斑是否已进入画面

可临时降低阈值：

```json
"threshold": 180
```

若 Web 状态中 `valid` 一直为 `0`，表示线程正在运行但未检测到有效光斑。

### 10.3 红色 `X` 不在光斑中心

检查：
- `vpss_chn` 是否与 Web 预览通道一致
- `window_size` 是否过大
- `threshold` 是否过低
- 光斑是否严重饱和
- 画面中是否有强反光

建议尝试：

```json
"window_size": 48,
"threshold": 240
```

### 10.4 清晰度曲线不更新

检查：
- `centroid_alg` 是否为 `1`
- Live 页面左上角 `age_ms` 是否持续变化
- `valid` 是否为 `1`
- `threshold` 是否导致光斑未被检测
- `clarity_window_w` / `clarity_window_h` 是否设置过小

若画面中没有有效光斑，曲线不会加入新样本。

### 10.5 MMZ 分配失败

错误示例：

```text
mmz_userdev: ioctl_mmb_alloc failed
```

当前版本已通过缓存复用降低 MMZ 压力。若仍出现该问题，建议重启开发板，清理旧进程占用的 MMZ。

### 10.6 曝光仍然过大

优先降低目标比例：

```json
"ae_target_ratio": 0.7
```

再限制最大曝光：

```json
"ae_max_exp_us": 10000
```

若光斑很亮，可进一步降低：

```json
"ae_target_ratio": 0.6
```

---

## 11. 快速操作流程

### 11.1 完整的现场使用流程

1. 确认 PC 与开发板网络连通（`ping 192.168.0.2`）
2. 浏览器打开 `http://192.168.0.2`
3. 进入 `Config -> GSF_ID_SVP_CENTROID_CFG`
4. 点击 `GET` 获取当前配置
5. 设置 `"centroid_alg": 1`
6. 根据现场需要调整 `threshold`、`window_size`、`ae_target_ratio`
7. 点击 `SET` 保存
8. 进入 `Live` 查看红色 `X` 和清晰度曲线
9. 若质心偏移，优先减小 `window_size` 并适当增大 `threshold`
10. 若光斑过亮，优先降低 `ae_target_ratio` 或 `ae_max_exp_us`

### 11.2 快速诊断步骤

```
网络不通 → 检查网线和 IP
Web 无法打开 → 检查网络 + 板端 webs.exe 进程
红色 X 不在光斑中心 → 检查 vpss_chn + 减小 window_size + 增大 threshold
没有质心结果 → 降低 threshold + 确认光斑进入画面
曲线不更新 → 确认 valid=1 + 检查 threshold
曝光过亮 → 降低 ae_target_ratio
```

---

## 12. 编译与升级

### 12.1 编译环境

在 WSL（Windows Subsystem for Linux）中操作：

```bash
cd /mnt/d/hisi/HIVIEW-master
```

加载编译环境：

```bash
source build/3516d
```

### 12.2 完整编译

```bash
make
```

### 12.3 仅重编 SVP（AI 算法）

```bash
make -C mod/svp DEPS=
```

### 12.4 仅重编 Web 页面和接口

```bash
make -C mod/webs DEPS=
```

### 12.5 修改 IPC / JSON 结构后强制清理重编

```bash
make -C mod/svp clean
make -C mod/svp DEPS=
make -C mod/webs clean
make -C mod/webs DEPS=
```

### 12.6 生成升级包

```bash
./ins.sh 16d
```

生成文件路径：

```text
D:\hisi\HIVIEW-master\upg\cam16d.upg
```

### 12.7 固件升级

1. PC 与开发板处于同一网段
2. 浏览器打开 `http://192.168.0.2`
3. 进入固件升级页面
4. 选择上传文件：`D:\hisi\HIVIEW-master\upg\cam16d.upg`
5. 等待上传完成并重启开发板

> **建议**：光斑质心功能同时修改了 `svp.exe`、`webs.exe` 和 Web 页面，建议使用完整 `.upg` 升级包，不要单独替换单文件。

---

## 附录

### A. Web 配置接口格式

获取配置（GET）：

```text
/config?id=GSF_ID_SVP_CENTROID_CFG&args=G0C0S0
```

保存配置（SET）：

```text
/config?id=GSF_ID_SVP_CENTROID_CFG&args=G1C0S0
```

获取状态（只读）：

```text
/config?id=GSF_ID_SVP_CENTROID&args=G0C0S0
```

### B. 联系方式

深圳市海图视讯科技有限公司（HIVIEW-TECH）
- 邮件：thomas@hiview-tech.cn
- 更多产品：https://github.com/openhisilicon/PRODUCT

---

*本手册基于 HIVIEW-master 版本编写，内容如有更新请以实际版本为准。*
